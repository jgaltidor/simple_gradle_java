plugins {
    id "java"
    id "application"
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation('junit:junit:4.13')
}

application {
    mainClass = 'jga.example.gradle.Main'
}

jar {
    manifest {
        attributes 'Main-Class': 'jga.example.gradle.Main'
    }
}

task uberJar (type: Jar) {
  archiveClassifier = 'uber'
  manifest {
    attributes 'Main-Class': 'jga.example.gradle.Main'
  }
  from {
    configurations.runtimeClasspath
      .findAll { it.name.endsWith('jar') }
      .collect { zipTree(it) }
  }
  with jar
}
